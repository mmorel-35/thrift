linters:
  disable:
    - errcheck
  enable:
    - asciicheck
    - errorlint
    - gci
    - gocritic 
    - gofumpt
    - govet
    - lll
    - nonamedreturns
    - revive
    - usestdlibvars

  linters-settings:
    gci:
      - standard
      - default
      - prefix(github.com/apache/thrift)
    govet:
      disable:
        - stdmethods
    lll:
      line-length: 100
run:
  skip-dirs:
    - tutorial
